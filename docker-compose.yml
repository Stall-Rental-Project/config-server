version: '3.8'
services:
  config-server-dev:
    image: mhmarket/config:latest
    container_name: config-server
    ports:
      - "8888:8888"
    restart: unless-stopped
    dns:
      - 192.168.200.4
      - 8.8.8.8
networks:
  mhmarket-config-dev:
    driver: bridge
